# Визуальные эффекты по блокам (по референсам)

Цвета не трогаю — остаются teal/emerald/slate/rose. Добавляю слои: декоративные «плавающие» элементы, мини-UI оверлеи, мягкие halo/glow, тонкие анимации `float`/`pulse`. Всё на CSS + SVG, без новых либ.

## Глобально (styles.css)
- Keyframes: `float-slow`, `float-soft`, `tilt`, `halo-pulse`, `marquee`.
- Утилиты: `.halo` (мягкое radial-glow за элементом), `.chip-3d` (плашка с микро-тенью + бордером), `.grid-dot` (точечный фон секции), `.soft-grid` (slate-сетка), `.noise` (тонкий шум 4%).
- `.float-a/.float-b/.float-c` — плавающие декоры с разной задержкой.

## По блокам (src/routes/index.tsx)

1. **Hero** — за фото клиники teal-halo + dot-grid фон. Поверх hero, по углам:
   - chip «+30 млн ₸ / 30 дней» (emerald, float-a),
   - chip «85% доходимость» (teal, float-b),
   - мини-карточка «Новый пациент · Имплантация» с зелёной точкой (float-c).
   Под H1 — узкая trust-полоса с лого-плашками клиник (заглушки текстом).

2. **Кейс Бакыта (01)** — карточка получает фоновый soft-grid + поверх «Operations»-оверлей в правом верхнем углу: мини-список из 3 строк «Конс. → Имплант → Контроль» с галочками. Слева снизу — float chip «13 млн ₸» emerald.

3. **ДО (02a)** — rose-фон + диагональные красные стрелки вниз (SVG), float chip «-40% доходимость».

4. **ПОСЛЕ (02b)** — teal-фон + «купюры» (SVG банкнот ₸) float-soft по углам, большая цифра 30 млн с halo-pulse.

5. **Эксперт Юрий (03)** — карточка-цитата в стиле Telegram: «шапка» с аватаром, временем, галочкой прочитано; бабл-сообщение с teal-акцентом; справа float chip «20+ клиник».

6. **4 проблемы** — каждая карточка с номером в teal-кружке + tilt при hover + красная точка-индикатор в углу.

7. **Шаги 1–3** — большой номер с halo за ним; «Пример» как мок-чата/CRM (две строки сообщений), «Результат» как мини-метрика с ▲ emerald.

8. **Стоимость (09)** — карточка с teal-рамкой, «было 19 900 ₸» зачёркнуто rose линией, «4 990 ₸» emerald 64px с halo. Сверху ленточка «-75%».

9. **Бонусы (10)** — карточки с emerald «бесплатно» badge, иконка-подарок float.

10. **Честно (11)** — questions list + итоговая graphite-плашка с emerald цифрой и noise-текстурой.

11. **Гарантия (12)** — иконка-щит в teal-круге с двойным halo-pulse, под ней «100% возврат» chip.

12. **FAQ (13)** — рамки + шеврон teal, hover поднимает карточку (translateY -2px).

13. **CTA-секции** — soft-grid фон, WhatsApp кнопка с лёгким halo.

## Что НЕ делаю
- Не генерирую новых фото (всё CSS/SVG-декор).
- Не меняю палитру и тексты.
- Не добавляю либы анимаций.

После апрува — реализую за один проход и сниму скрин для проверки.
